import { ProjectDirective } from './project-directive.directive';

describe('ProjectDirective', () => {
  it('should create an instance', () => {
    const directive = new ProjectDirective();
    expect(directive).toBeTruthy();
  });
});
